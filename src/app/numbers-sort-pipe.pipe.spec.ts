import { NumbersSortPipePipe } from './numbers-sort-pipe.pipe';

describe('NumbersSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumbersSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
